/* 1.cm */
/* An error free C- program used to test the compiler's AST and symbol table functions */

int counter;

int printFabulous(int n) {
    int counter;

    if (n == 1) {
        int counter;
        counter = 1;
    }

    counter = 8;
    return counter;
}

void main(void) {
    int counter;
    counter = printFabulous(1);
    output(counter);
}
