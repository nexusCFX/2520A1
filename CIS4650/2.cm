/* 2.cm */

/* A trivial C- program which should compile without errors. */

int daysLeftInYear = 365;

void countDownToNewYearsFrom(int daysToDate) {
    daysLeftInYear = daysLeftInYear - daysToDate;
    while (daysLeftInYear >= 0) {
        output(daysLeftInYear);
        daysLeftInYear = daysLeftInYear - 1;
    }
}

void main(void) {
    int daysToDate;
    daysToDate = input();
    countDownToNewYearsFrom(daysToDate);
}