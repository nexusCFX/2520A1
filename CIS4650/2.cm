/* 2.cm */

/* A C- program that tests against undefined and redefined variables and undefined functions */

int word[100];
int shiftWord[100];

void populateWord(void) {
    int i;
    i = 0;
    while (i < 100) {
        word[i] = input();
        i = i + 1;
    }
}

int main(void) {
    int shiftWord[100];
    int i;
    int i;
    i = 0;
    populateWord();
    while (i < 100) {
        int shiftedAscii;
        shiftedAscii = shift(3, word[i]);
        shiftWord[i] = shiftedAscii;
        i = i + 1;
    }

    /* Output encrypted text */
    j = 0;
    while (i < 100) {
        output(shiftWord[i]);
        i = i + 1;
    }
}

int shift(int i, int ascii) {
    int shiftedAscii;
    shiftedAscii = ascii;
    return shiftedAscii + i;
}
