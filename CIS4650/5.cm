/* 5.cm */

/* A trivial C- program which contains the following:
   -> Comment syntax error
   -> No semi-colon error
*/
int balance;

/* Increments global balance variable by 100 * /
void increaseBalance() {
    int n
    
    while (n <= 100) {
        balance = balance + 1;
        n = n + 1;
    }
}

int main(void) {
    balance = input();

    if (balance < 0) {
        return -1;
    }
    if (balance == 0) {
        increaseBalance();
        return 1;
    }

    return 1;
}
